# Simple blockchain in Go

## Install
### Local
`go run main.go`
### Using docker
`make dev-image`

`docker-compose up -d`

## Description
This simple blockchain application in Go is 
`http://localhost:8080`